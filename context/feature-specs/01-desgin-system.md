Read `AGENTS.md` before starting.

We're adding the design system and UI primitve components.

Install and configer `shadcn/ui`.

add these shadcn components:
- Button
- Card
- Dialog
- Input
- Tabes
- Textarea
- ScrollArea

Do not modify the generated `components/ui/*` files installation.

Also install `Lucide-react`.

Create `Lib/utils.ts` with a reusable `cn()` helper for merging Tailwind classes.

Ensure all components match the existing dark theme in `globals.css`.

### Check when done

- All components impoer without errors
- `cn()` works properly
- No default light styling appears